@page
@using eiibd26.Models.Platillos
@model eiibd26.Areas.Identity.Pages.Admin.Platillos.AtributosModel
@{
    ViewData["Title"] = "Platillos · Atributos";
}

@section Styles {
    <link rel="stylesheet" href="~/css/admin-grid.css" asp-append-version="true" />
}

<div class="eii-page eii-page--wide">
    <div class="d-flex justify-content-between align-items-center mb-1">
        <h1 class="eii-page-title eii-page-title--sm mb-0">Atributos</h1>
        <a class="eii-btn eii-btn--primary" asp-page="AtributoDetalle" target="_blank" rel="noopener">
            <i class="bi bi-plus-lg" aria-hidden="true"></i> Nuevo atributo
        </a>
    </div>
    <p class="eii-form-text mb-4">
        Cómo se comporta un alimento. <strong>Ingrediente</strong> = intrínseco, cómo es siempre (gluten, picante, cítrico).
        <strong>Uso</strong> = cómo se usa en un platillo (crudo, frito, en jugo).
    </p>

    @if (!string.IsNullOrEmpty(Model.SuccessMessage))
    {
        <div class="alert alert-success" role="alert">@Model.SuccessMessage</div>
    }
    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
    {
        <div class="alert alert-danger" role="alert">@Model.ErrorMessage</div>
    }

    <div class="eii-admin-grid-wrap">
        @* Sin data-eii-grid-order: el servidor ordena por Ámbito y luego Nombre
           (dos columnas), y el componente respeta ese orden por defecto. *@
        <table id="gridAtributos"
               class="table table-hover align-middle mb-0"
               data-eii-grid
               data-eii-grid-empty="Aún no hay atributos.">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Ámbito</th>
                    <th>Descripción</th>
                    <th>Estado</th>
                    <th data-no-sort class="text-end">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var a in Model.Items)
                {
                    <tr>
                        <td>@a.Nombre</td>
                        <td><span class="eii-badge">@a.Ambito</span></td>
                        <td class="text-muted">@a.Descripcion</td>
                        <td>
                            @if (a.Activo)
                            {
                                <span class="eii-badge">Activo</span>
                            }
                            else
                            {
                                <span class="text-muted">Inactivo</span>
                            }
                        </td>
                        <td class="text-end">
                            <a class="eii-btn eii-btn--ghost eii-btn--sm"
                               asp-page="AtributoDetalle" asp-route-id="@a.Id"
                               target="_blank" rel="noopener">Editar</a>
                            <form method="post" asp-page-handler="ToggleActivo" asp-route-id="@a.Id" class="d-inline">
                                <button type="submit" class="eii-btn eii-btn--link eii-btn--sm">@(a.Activo ? "Desactivar" : "Reactivar")</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.datatables.net/1.13.7/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.7/js/dataTables.bootstrap5.min.js"></script>
    <script src="~/js/admin-grid.js" asp-append-version="true"></script>
}
